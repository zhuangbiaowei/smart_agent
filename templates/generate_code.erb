Write a Ruby function called "<%= name %>" that <%= description %>. The function should accept <%= input_params_type %> as input and return <%= output_value_type %>.
Use regular parameters, not named arguments. Just return the source code to me, no other explanations needed.